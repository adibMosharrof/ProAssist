"""Dataset helpers package for DST generator.

This package exposes the `DataSourceFactory` which returns self-contained
dataset helpers (`ManualDSTDataset`, `ProAssistDSTDataset`). Legacy DataSource
classes were removed in favor of these dataset implementations.
"""
